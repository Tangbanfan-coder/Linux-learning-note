# Linxu学习笔记(默认家目录是/home/zhou)

## linux基础知识部分

```bash
1.命令是可以进行简写的，如'ls -a -h -l'可以简写为'ls -ahl'，如果参数单一，如'ls -a'，甚至可以简写为'la'，效果相同。不过，后者需要以一配置文件实现。

2.linux中，'-'和'--'的最大区别是前者后面接的一般是短选项，如'-a'，而后者后面接的一般是长选项，如'--help'。

3.选项的形式如'-a''-l'，参数即路径如/usr/bin

4. 相对路径和绝对路径：使用相对路径即不打全路径，如在使用'cd'命令时，使用'cd test',就是直接进入当前目录下的'test'文件。而绝对路径是以'/'开头的，需要打全路径，如/home/zhou/test。

5.$()的效果和``效果相同，让bash把其中包含的内容当作一个命令解析，现在更建议使用$()

6.通配符'*',这个*表示可以换成任何内容，比如find / -name test*就表示寻找任何以test开头的文件，*test则相反，是任何以test结尾的文件，*test*则表示任何包含test的文件

7.''和""的区别：在''中，bash不会展开其中包含的任何内容，比如我们先写name="hello"，再写echo 'S(name)'输出的就是$(name),但是如果使用""就会输出hello,在使用两者时，需要注意想不想要展开内部包含的内容
```

## 基础命令部分

### ls命令：

```bash
作用：

可以查看输入的参数内部的文件和文件夹，默认不展示隐藏文件和文件夹

对应的选项:

-a让输出的结果包含隐藏文件及文件夹，一般为'.'开头的文件及文件夹，如'.cache'

-l以列表的形式输出结果，让本来横向排列输出结果变为竖向排列，并额外展示权限，大小，创建的日期等内容。开头会有如'd'开头和'-'形式的文本，如果以'd'开头，则表示是一个文件夹，如果以'-'开头，则表示是一个文件。

-h需要和-l配合使用，在使用-l展示文件详细数据时，文件大小部分可以看到文件大小的单位，如'K','M','G'
```

### cd命令

```bash
作用：

进入对应的目录即文件夹

使用方式：cd 路径 

如cd /home/zhou

额外内容：

'.'表示当前目录，即使用'cd ./zhou'的效果和'cd zhou'的效果是相同的(假设当前处于home目录下)

'..'表示上一级目录，即使用'cd ..'可以回到上一级目录，也可以连续使用，比如'cd ../..'

'~'表示家目录，如果使用'cd ~'就会直接回到'/home/zhou'，也可以写'cd ~/zhou目录下的某个目录'来实现快速的访问

```
### pwd命令

```bash
作用：

查看当前自己正处于哪个目录

使用方式：直接输入pwd即可

假如正处于/home/zhou目录下，输入pwd就会直接输出/home/zhou，可以用来快速确认自己正处于哪个目录下

```
### mkdir命令

```bash
作用：

创建一个目录

使用方式：mkdir 路径 

如果路径填写/home/zhou/test，就会在家目录下创建一个test目录。

选项：-p，可以连续创建目录。例子：如果我们想要连续创建目录，就需要增加这个选项，如果我们写mkdir /home/zhou/test/test2时，test目录还没有被创建，就会报错，但是如果加上-p选项，test和test2目录就会被同时创建

```
### touch命令

```bash
作用：

创建一个文件

使用方法：touch 路径 

和mkdir相似，会在指定的目录下创建一个文件，如touch /home/zhou/test.txt，就会在家目录下创建一个名为test的文本文件。

```
### cat命令

```bash
作用:

查看文件内部的内容

使用方法：cat 路径 

查看指定路径下某个指定文件的内容

cat /home/zhou/test.txt（假如test.txt已存在，并且其中包含'hello world'），就会输出'hello world'
```

### more命令

```bash
作用：

和cat类似，也是查看文件内部的内容，但也有区别，在文件内部数据量小的时候区别无法体现出来，但是在数据量大起来之后就能体现出区别：可以翻页(按空格翻页)

使用方式 more 路径
```
### cp命令

```bash
全称是cpoy，即复制的意思

作用：

复制一个文件或者文件夹到另一个位置

使用方法：cp 选项 路径 路径

两个路径中，前者是被复制的文件或者文件夹，后者是要复制到的位置

选项：-r，只有添加这个选项才能复制文件夹，否则默认状态下只能复制文件。
```

### mv命令

```bash
全称是move，即移动

作用：

把一个文件或者一个文件夹移动到指定的位置，或者可以对文件或者文件夹进行改名的操练。

使用方法: move 路径 路径

两个路径中，前者是被移动的文件或者文件夹，后者则是要移动到的位置，如果后者不存在，则会把前者更名为后者
```

### rm命令

```bash
全称是remove

作用：删除一个文件夹或者一个文件

使用方法：rm 选项 参数1 参数2......

这里的参数可以无限写下去，只要对应的文件夹和文件存在就能删掉(权限足够)，也就是说可以实现批量删除

选项：

-r和cp命令类似，需要增加-r选项才能删除文件夹，否则只能删除文件。

-f表示强制删除，增加这个选项之后，无论删除的路上遇到什么问题，都不会报错，即使文件不存在，权限不足等，一般给管理员使用
```
### which命令

```bash
作用：查询某个程序存放在哪里

我们使用的命令本质上是一个程序，比如ls命令和cat命令，此时我们想要知道这些程序存放在哪里，就可以使用which命令进行查询

使用方式：which 程序名 如which ls

注意：有些命令是shell内置命令，使用which是查不到的，此时可以使用type命令查看命令的类型(是一个内置命令还是一个外部命令)
```
### find命令

```bash
作用：查询某个文件所处的位置

find 起始路径 -选项 文件名/文件大小

文件名可以搭配通配符进行使用

起始路径就是从哪里开始找，如果写/就是全盘搜索，如果写/home，就是在家目录中进行搜索

选项主要有-name 和 -size 两个选项，前者表示按名称进行查找，后者表示按文件大小进行查找。后者需要特别注意，写大小时需要些+或者-号，前者表示是大于这个大小的文件，后者表示查找小于这个大小的文件，大小的单位有K，M，G，分别表示KB,MB和GB

```

### grep命令

```bash
作用：筛选文件中内容

使用方式：grep 选项 要查找的关键字 路径

要查找的关键字即你想要筛选的内容

选项：-n，输出结果的同时，会标注这行内容出现在被筛选的文件中的第几行
```

###  wc命令

```bash
作用：统计文件中的字符数，所占字节数，行数，单词数等数据。

使用方法：wc 选项 路径

选项:
-c 统计所有内容所占字节数
-m 统计所有内容有多少个字符
-l 统计内容一共有几行
-w 统计内容一共有几个单词(按照空格划分单词)

如果不打任何选项，则默认输出行数，单词数，字节数
```

### 管道符:|

```bash
作用，把一个命令的输出当作另一个命令的输入

使用方法：一个命令 | 一个命令

例子：cat test.txt | grep hello 

此时你会发现grep的路径没有填写，但是不会报错，因为管道符把cat命令输出的内容作为输入传给了grep命令

相似的用法还有很多
```

### echo命令

```bash
作用：输出后面跟着的内容

用法：echo 内容

```
### >和>>

```bash
作用：覆盖写入文件或者追加写入文件(前者是覆盖写入，后者是追加写入)

使用方法: >/>>路径

如echo "hello world" >/>> test.txt

```

### tail命令

```bash
作用：查看文件尾部的内容

使用方法： tail 选项 路径

选项：
-f表示持续查看，假如我们再开一个窗口对路径指向的文件进行修改，输入命令的窗口中会立即展示变化
-num(num指任意数字，如1，2，3)指定查看的行数，比如tail -3 test.txt就只会查看三行了

```
## vi/vim编辑器

```bash
两者都是修改文件用的编辑器

启用方式：vi/vim 路径

现在多使用且更建议使用vim，因为功能更加强大，下面也只拿vim举例

在vim编辑器中，有三种模式：命令模式，编辑模式，底线模式

命令模式：任何的输入都会被shell理解为命令，也是最基础的模式，打开文件时默认为命令模式

编辑模式：输入会被理解为对文件的改动，此时可以修改文件

底线模式：一般用于进行保存和退出的操作

基本的使用：刚打开文件时，是命令模式，按下'i'可在当前光标所处位置进入编辑模式，此时可以进行编辑文件操作，想要退回命令模式，按下键盘上的'Esc'键即可，在命令模式下，按下':'即可进入底线模式，此时输入'w'再按下回车会保存文件，按下'q'会退出文件，可以连载一起输入如'wq'，就是保存并退出。如果文件被修改了，但是直接输入'q'将无法退出，需要在'q'的末尾加上'!'才可退出，即'q!'。底线模式下，输入set nu，可以显示行号，输入set pause，可以设置为粘贴模式，用于保证从外部复制来的内容粘贴进来不会导致格式混乱。

```